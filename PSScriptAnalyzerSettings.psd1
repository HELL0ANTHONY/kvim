@{
	Rules = @{
		PSUseConsistentWhitespace = @{
			Enable = $true
		}
		PSUseConsistentIndentation = @{
			Enable = $true
		}
		PSUseLineLength = @{
			Enable = $true
			LineLength = 120 # Cambia a 80 o cualquier límite que prefieras
		}
	}
}
